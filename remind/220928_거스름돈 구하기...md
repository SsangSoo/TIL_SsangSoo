```
package com.codestates.coplit; 
import java.util.*;

public class Solution { 
	public int partTimeJob(int k) {
    // TODO:
    int[] money = new int[]{1, 5, 10, 50, 100, 500};  // 오름차순으로 정렬.(동전 종류)

    int count = 0; // 개수 반환할 int형 변수

    for(int i=money.length-1; i>=0; i--) {
      System.out.println("동전 =" + money[i]);
        if(k >= money[i]) {                        // k원이 money[i]보다 크면,
          int tmp = k % money[i];   // k에서 money[i]를 나눈 나머지를 tmp에
          count += (k - tmp) / money[i];  // 원래 금앣에서 money[i]로 나눈 나머지를 뺀금액을 money[i]로 나눈 몫을 count에 더함.
          k = k%money[i]; // k에서 money[i]를 나눈 나머지를 k로 저장.
        } else continue; // 없다면, 다음 요소.
    } // for
    return count;
	} 
}
```
k원을 거스름돈 동전으로 줘야할 때...와 관련된 문제
